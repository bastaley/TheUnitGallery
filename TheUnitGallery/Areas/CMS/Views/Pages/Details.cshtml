@model TheUnitGallery.Models.Page

@{
    ViewBag.Title = "Details";
}

<div class="row body-content">
    @Html.Partial("_CMSAdminNavPanel")
    <div class="col-sm-9 col-md-10">
        <h2>Page Details</h2>
        <hr />

        <div>
            <dl class="dl-horizontal">
                <dt>
                    @Html.DisplayNameFor(model => model.Title)
                </dt>

                <dd>
                    @Html.DisplayFor(model => model.Title)
                </dd>

            </dl>
        </div>
        <p>
            @Html.ActionLink("Edit", "Edit", new { id = Model.Identifier }, new { @class = "btn btn-outline-primary" })
            @Html.ActionLink("Back to List", "Index", new { }, new { @class = "btn btn-outline-primary" })
        </p>
        </div>
    </div>
